var animals = ['Cat', 'Dog', 'Elephant', 'Bee', 'Ant'];

var check = Array.isArray(animals); 
console.log(check);

if (Array.isArray (animals)) {
    document.write("This is an Array"); 
} else {
    document.write("This is not an Array"); 
}